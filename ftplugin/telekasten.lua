-- local keymap = vim.keymap.set
-- local bufnr = vim.api.nvim_get_current_buf()

-- local glow = function()
  -- local bufid = vim.api.nvim_get_current_buf()
  -- local buf = vim.api.nvim_buf_get_name(bufid)
  -- local cmd = "FloatermNew glow -p " .. buf
  -- print(cmd)
  -- vim.cmd(cmd)
-- end

-- keymap('n', '<Space>G', glow, { buffer = bufnr, desc = 'preview note file' })
